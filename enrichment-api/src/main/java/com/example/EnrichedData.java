package com.example;

public class EnrichedData {
    private final byte[] enrichedData;

    public EnrichedData(byte[] enrichedData) {
        this.enrichedData = enrichedData;
    }

    public byte[] getEnrichedData() {
        return enrichedData;
    }
}
